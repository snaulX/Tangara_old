use System;
use System.Collections;

Console.writeLine("Hello Mino!");

define class mycol(T, varType1 : struct) { // T is class
    T : ICollection; // set that class will implements from ICollection
    var varType1 T.a;
};

mycol(int) MyCollection(int a) {
    ctor {
        this.a = a;
    }
}

mycol(string) Error; // will be error becouse string is not struct